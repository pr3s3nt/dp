metadata:
  name: prometheus-cluster-monitoring
  namespace: cattle-prometheus
  labels:
    app: prometheus
    chart: prometheus-0.0.1
    heritage: Tiller
    io.cattle.field/appId: cluster-monitoring
    monitoring.coreos.com: 'true'
    release: cluster-monitoring
  annotations:
    project.cattle.io/namespaces: '["cattle-fleet-clusters-system","cattle-fleet-local-system","cattle-fleet-system","cattle-global-data","cattle-global-nt","cattle-impersonation-system","cattle-logging","cattle-prometheus","cattle-system","cert-manager","cluster-fleet-local-local-1a3d67d0a899","fleet-default","fleet-local","fleet-system","ingress-nginx","kube-node-lease","kube-public","kube-system"]'
    prometheus-operator-input-hash: '10610086161518073319'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
      chart: prometheus-0.0.1
      monitoring.coreos.com: 'true'
      prometheus: cluster-monitoring
      release: cluster-monitoring
  template:
    metadata:
      labels:
        app: prometheus
        chart: prometheus-0.0.1
        monitoring.coreos.com: 'true'
        prometheus: cluster-monitoring
        release: cluster-monitoring
    spec:
      volumes:
      - name: config
        secret:
          secretName: <REDACTED:env>
          defaultMode: 420
      - name: tls-assets
        secret:
          secretName: <REDACTED:env>
          defaultMode: 420
      - name: config-out
        emptyDir: {}
      - name: prometheus-cluster-monitoring-rulefiles-0
        configMap:
          name: prometheus-cluster-monitoring-rulefiles-0
          defaultMode: 420
      - name: configmap-prometheus-cluster-monitoring-nginx
        configMap:
          name: prometheus-cluster-monitoring-nginx
          defaultMode: 420
      - name: nginx-home
        emptyDir: {}
      containers:
      - name: prometheus
        image: rancher/prom-prometheus:v2.18.2
        args:
        - --<DOMAIN_720>=/etc/prometheus/consoles
        - --<DOMAIN_721>=/etc/prometheus/console_libraries
        - --<DOMAIN_722>=/etc/prometheus/config_out/<DOMAIN_723>
        - --<DOMAIN_724>=/prometheus
        - --<DOMAIN_725>=720h
        - --<DOMAIN_726>-lifecycle
        - --<DOMAIN_727>-lockfile
        - --<DOMAIN_728>-prefix=/
        - --<DOMAIN_560>-address=127.0.0.1:9090
        resources:
          limits:
            cpu: '2'
            memory: 2Gi
          requests:
            cpu: '1'
            memory: 1Gi
        volumeMounts:
        - name: config-out
          readOnly: true
          mountPath: /etc/prometheus/config_out
        - name: tls-assets
          readOnly: true
          mountPath: /etc/prometheus/certs
        - name: prometheus-cluster-monitoring-db
          mountPath: /prometheus
          subPath: prometheus-db
        - name: prometheus-cluster-monitoring-rulefiles-0
          mountPath: /etc/prometheus/rules/prometheus-cluster-monitoring-rulefiles-0
        - name: configmap-prometheus-cluster-monitoring-nginx
          readOnly: true
          mountPath: /etc/prometheus/configmaps/prometheus-cluster-monitoring-nginx
        livenessProbe:
          exec:
            command:
            - sh
            - -c
            - if [ -x "$(command -v curl)" ]; then curl http://localhost:9090/-/healthy;
              elif [ -x "$(command -v wget)" ]; then wget -q -O /dev/null http://localhost:9090/-/healthy;
              else exit 1; fi
          timeoutSeconds: 3
          periodSeconds: 5
          successThreshold: 1
          failureThreshold: 6
        readinessProbe:
          exec:
            command:
            - sh
            - -c
            - if [ -x "$(command -v curl)" ]; then curl http://localhost:9090/-/ready;
              elif [ -x "$(command -v wget)" ]; then wget -q -O /dev/null http://localhost:9090/-/ready;
              else exit 1; fi
          timeoutSeconds: 3
          periodSeconds: 5
          successThreshold: 1
          failureThreshold: 120
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: FallbackToLogsOnError
        imagePullPolicy: IfNotPresent
      - name: prometheus-config-reloader
        image: rancher/coreos-prometheus-config-reloader:v0.38.1
        command:
        - /bin/prometheus-config-reloader
        args:
        - --log-format=logfmt
        - --reload-url=http://localhost:9090/-/reload
        - --config-file=/etc/prometheus/config/<DOMAIN_729>
        - --config-envsubst-file=/etc/prometheus/config_out/<DOMAIN_723>
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: <DOMAIN_21>
        resources:
          limits:
            cpu: 100m
            memory: 25Mi
          requests:
            cpu: 100m
            memory: 25Mi
        volumeMounts:
        - name: config
          mountPath: /etc/prometheus/config
        - name: config-out
          mountPath: /etc/prometheus/config_out
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: FallbackToLogsOnError
        imagePullPolicy: IfNotPresent
      - name: rules-configmap-reloader
        image: rancher/jimmidyson-configmap-reload:v0.3.0
        args:
        - --webhook-url=http://localhost:9090/-/reload
        - --volume-dir=/etc/prometheus/rules/prometheus-cluster-monitoring-rulefiles-0
        resources:
          limits:
            cpu: 100m
            memory: 25Mi
          requests:
            cpu: 100m
            memory: 25Mi
        volumeMounts:
        - name: prometheus-cluster-monitoring-rulefiles-0
          mountPath: /etc/prometheus/rules/prometheus-cluster-monitoring-rulefiles-0
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: FallbackToLogsOnError
        imagePullPolicy: IfNotPresent
      - name: prometheus-proxy
        image: rancher/library-nginx:1.19.2-alpine
        command:
        - /bin/sh
        - -c
        - cp /nginx/<DOMAIN_730> /var/cache/nginx/<DOMAIN_731>; chmod +x /var/cache/nginx/<DOMAIN_731>;
          /var/cache/nginx/<DOMAIN_731>
        ports:
        - name: nginx-http
          containerPort: 8080
          protocol: TCP
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: <DOMAIN_165>
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 50m
            memory: 50Mi
        volumeMounts:
        - name: configmap-prometheus-cluster-monitoring-nginx
          mountPath: /nginx
        - name: nginx-home
          mountPath: /var/cache/nginx
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 101
          runAsGroup: 101
      - name: prometheus-agent
        image: rancher/prometheus-auth:v0.2.0
        command:
        - prometheus-auth
        args:
        - --proxy-url=http://127.0.0.1:9090
        - --listen-address=$(POD_IP):9090
        - --filter-reader-labels=prometheus
        - --filter-reader-labels=prometheus_replica
        ports:
        - name: web
          containerPort: 9090
          protocol: TCP
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: <DOMAIN_165>
        resources:
          limits:
            cpu: 500m
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 100Mi
        livenessProbe:
          httpGet:
            path: /-/healthy
            port: web
            scheme: HTTP
          initialDelaySeconds: 300
          timeoutSeconds: 10
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /-/ready
            port: web
            scheme: HTTP
          initialDelaySeconds: 60
          timeoutSeconds: 10
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 10
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 600
      dnsPolicy: ClusterFirst
      nodeSelector:
        kubernetes.io/os: linux
      serviceAccountName: cluster-monitoring
      serviceAccount: cluster-monitoring
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
        fsGroup: 2000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: prometheus
                  prometheus: cluster-monitoring
              topologyKey: <DOMAIN_138>/hostname
      schedulerName: default-scheduler
      tolerations:
      - key: <DOMAIN_658>/os
        operator: Equal
        value: linux
        effect: NoSchedule
  volumeClaimTemplates:
  - kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: prometheus-cluster-monitoring-db
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 100Gi
      storageClassName: ceph-block-replica-2
      volumeMode: Filesystem
      dataSource:
        apiGroup: ''
        kind: ''
        name: ''
  serviceName: prometheus-operated
  podManagementPolicy: Parallel
  updateStrategy:
    type: RollingUpdate
  revisionHistoryLimit: 10
apiVersion: apps/v1
kind: StatefulSet
