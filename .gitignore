# score-k8s state (chứa password sinh ra) + secret tách ra từ manifest
.score-k8s/
secrets.yaml
work-*/
# workdir của scripts/test-local.sh (render + state + secret tách ra — không commit)
.sandbox/
# workdir của scripts/deploy-do.sh (render + state + secret tách ra — không commit)
.do-deploy/

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
.terraform.lock.hcl

# node
node_modules/

# khác
kubeconfig*
.DS_Store
